// 1. Alert on language change
/*const langSelect = document.querySelector(".country-select");
langSelect.addEventListener("change", function () {
    alert("Language changed to: " + this.value);
});

// 2. Alert search text
const searchBtn = document.querySelector(".search-btn");
const searchInput = document.querySelector(".search-input");

searchBtn.addEventListener("click", function () {
    if (searchInput.value.trim() === "") {
        alert("Please enter something to search.");
    } else {
        alert("You searched for: " + searchInput.value);
    }
});

// 3. Scroll to top smoothly
const backToTop = document.querySelector(".back");
backToTop.addEventListener("click", function () {
    window.scrollTo({
        top: 0,
        behavior: "smooth"
    });
});

// 4. (Optional) Sign in dropdown on hover
const signin = document.querySelector(".signin");
signin.addEventListener("mouseenter", function () {
    signin.style.backgroundColor = "#131921";
});
signin.addEventListener("mouseleave", function () {
    signin.style.backgroundColor = "transparent";
});

// 5. (Optional) Fake product click alert
const shopLinks = document.querySelectorAll(".box-content p");
shopLinks.forEach((p) => {
    p.addEventListener("click", () => {
        alert("This link doesn't go anywhere yet.");
    });
});*/
